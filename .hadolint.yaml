---
# DL3018: Pin versions in apk add — Alpine moves too fast, we want latest patches.
# DL3003: Use WORKDIR to switch to a directory — legit in multi-step RUN with cleanup.
# DL4005: Use SHELL to change the default shell — we create /bin/sh symlink for varnishd system() calls, not for RUN.
ignored:
  - DL3018
  - DL3003
  - DL4005
